{
  "name": "homer-project",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm run start",
    "build": "cd backend && npm run build && cd ../frontend && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
